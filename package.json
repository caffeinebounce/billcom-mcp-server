{
  "name": "@billcom/billcom-mcp-server",
  "version": "0.0.1",
  "description": "MCP server implementation for Bill.com integration",
  "license": "MIT",
  "main": "dist/index.js",
  "type": "module",
  "bin": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.0",
    "dotenv": "^16.4.7",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@vitest/coverage-v8": "^3.0.0",
    "shx": "^0.3.4",
    "typescript": "^5.8.2",
    "vitest": "^3.0.0"
  }
}
